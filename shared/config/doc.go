// config package from micro-strter-kit
// import myConfig "github.com/seidu626/audiobook/shared/config" to `main.go` and all test files.
// add myConfig.InitConfig(configDir, configFile), config.Scan(&cfg) in service.Init()
package config
